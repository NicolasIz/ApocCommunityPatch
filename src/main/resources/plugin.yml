name: ArkcronistGenerator
version: '${project.version}'
main: com.arkcronist.gen.bukkit.ArkcronistPlugin
api-version: '1.21'
description: Advanced procedural world generator - continents, deep oceans, mountains, canyons, caves, structures and minibosses.
author: Arkcronist
website: https://github.com/NicolasIz/ApocCommunityPatch
folia-supported: false

# Optional, both of them. Without MythicMobs the hostile-mobs section does nothing and the world
# keeps its vanilla mobs; ModelEngine is what gives those mobs their models, and MythicMobs is the
# one that asks for it, so this plugin never touches it directly.
softdepend: [MythicMobs, ModelEngine]

commands:
  ag:
    description: ArkcronistGenerator control command.
    usage: /ag help
    aliases: [arkgen, arkcronist]
    permission: arkcronist.command

permissions:
  arkcronist.command:
    description: Use /ag and its informational subcommands.
    default: op
  arkcronist.admin:
    description: Reload the configuration.
    default: op
